rvm:
- 2.1.0
notifications:
  irc:
    channels:
      - "irc.freenode.net#theodi"
    template:
      - "%{repository} %{branch} - %{message} %{build_url}"
    on_success: change
    on_failure: always
env:
  global:
    - BASE_URI='http://open-data-certificate.dev'
    - ODC_USERNAME=''
    - ODC_API_KEY=''
